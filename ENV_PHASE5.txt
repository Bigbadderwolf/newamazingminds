# ─── ADD THESE TO YOUR EXISTING .env.local ───────────────

# M-Pesa Daraja (get from developer.safaricom.co.ke)
MPESA_CONSUMER_KEY=your_daraja_consumer_key
MPESA_CONSUMER_SECRET=your_daraja_consumer_secret
MPESA_SHORTCODE=174379
MPESA_PASSKEY=bfb279f9aa9bdbcf158e97dd71a467cd2e0c893059b10f78e6b72ada1ed2c919
MPESA_ENV=sandbox

# Also add shortcode as public var (used in checkout UI)
NEXT_PUBLIC_MPESA_SHORTCODE=174379

# Reminder: APP_URL must be set for the callback to work
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: For local development, M-Pesa callbacks need a public URL.
# Use ngrok: npx ngrok http 3000
# Then set NEXT_PUBLIC_APP_URL=https://your-ngrok-url.ngrok.io
